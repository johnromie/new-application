# Concern Feature Implementation

## Plan Breakdown
1. [x] Update server/server.js: Add 'concerns' to db, POST/GET /api/concerns (done, but server.js has syntax error - fixed db.json).
2. [ ] Update employee/: Add concern modal/send to app.js/HTML.
3. [ ] Update admin/: Add message modal/list to app.js/HTML.
4. [ ] Icons: Proper SVG/emoji for topbar N/M/=.
5. [ ] Menu (=): "Need Help" contact + Log Out.
6. [ ] cap:sync both platforms.

Status: Server backend ready (/api/concerns works), next employee app.
